# Use monorepo root .env.example as the canonical template.
# Copy /project-management-tool/.env.example to /project-management-tool/.env
# and keep app-specific overrides in frontend/.env.local only if needed.
